# see http://about.travis-ci.org/docs/user/languages/php/ for more hints
language: php

# list any PHP version you want to test against
php:
   # aliased to a recent 5.6.x version
  - 5.6
  # aliased to a recent 5.3.x version
  - 5.3
  # hhvm
  #- hhvm

# use the $DB env variable to determine the phpunit.xml to use
before_script:
  - composer install --no-interaction
  - mkdir -p build/logs

script:
  - vendor/phpunit/phpunit/phpunit --configuration phpunit.travis.xml --coverage-clover build/logs/clover.xml

after_success:
    - php vendor/bin/coveralls -v
    - CODECLIMATE_REPO_TOKEN=5150b7bd6c0820bf9eb1af0b144e4e79ad5befe989449ada362935b72961c3c7 ./vendor/bin/test-reporter

# configure notifications (email, IRC, campfire etc)
notifications:
  email: "ashley@zf4.biz"